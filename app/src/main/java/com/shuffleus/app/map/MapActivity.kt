package com.shuffleus.app.map

import android.os.Bundle
import android.os.PersistableBundle
import androidx.appcompat.app.AppCompatActivity
import com.shuffleus.app.settings.AddPlayerFragment
import com.shuffleus.app.R

class MapActivity :AppCompatActivity() {
  /*  override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_map)

        supportFragmentManager.beginTransaction().add(R.id.settings_fragment_container, MapFragment.newInstance()).commit()

        supportActionBar?.setDisplayHomeAsUpEnabled(true)
    }*/
}